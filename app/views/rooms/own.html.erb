  # <div class="col-md-8">
  #   <% if @user.microposts.any? %>
  #     <h3>Microposts (<%= @user.microposts.count %>)</h3>
  #     <ol class="microposts">
  #       <%= render @microposts %>
  #     </ol>
  #     <%= will_paginate @microposts %>
  #   <% end %>
  # </div>

# 以下のメソッドをuser.rbに記載しえているから使用する

  # ユーザーモデルに記載する方がユーザー観点の方が記述しやすい。
  # 自分の作成した部屋を全て抽出する。
  # def my_room
  #   Room.where("user_id = ?", id)
  # end
